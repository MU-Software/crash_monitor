name: Dependency audit

on:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:
  pull_request:
    paths:
      - Cargo.toml
      - Cargo.lock
      - crates/**/Cargo.toml
      - crates/**/Cargo.lock

jobs:
  rustsec:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Install cargo-audit from its tested lockfile
        run: cargo install cargo-audit --version 0.22.2 --locked
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
