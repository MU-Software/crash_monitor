# Cargo configuration for the crash monitor crate.
#
# Unit tests allocate a process-unique temporary directory in `utils::paths`.
# Integration and E2E tests inject their own temporary output roots explicitly,
# so Cargo does not mutate process-global environment or share a fixed path.
